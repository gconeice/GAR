   # task: OUTPUT gcd(%GR1, %GR2) to GR1
      # BOTH (unsigned) POSITIVE
BB0:
EQ %GR3, %GR1, %GR2
JE %GR3, BB_END
CMP %GR3, %GR2, %GR1
JB %GR3, BB1
SWAP %GR1, %GR2
BB1:
SUB %GR1, %GR1, %GR2
J BB0
BB_END:
HALT